<UserControl x:Class="GameRoutines.CustomReminderWindowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GameRoutines"
             Foreground="{DynamicResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="20"
                   FontWeight="SemiBold"
                   Text="{Binding GameName, Mode=OneWay}"
                   TextTrimming="CharacterEllipsis" />
        <TextBlock Grid.Row="1" Margin="0,3,0,12" FontSize="15" Opacity="0.8" Text="Set up reminder" />
        <local:CustomReminderEditor Grid.Row="2" DataContext="{Binding EditableReminder}" />
        <StackPanel Grid.Row="3" Margin="0,18,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button MinWidth="84" Padding="12,5" IsDefault="True" Content="Save" Click="SaveButton_Click" />
            <Button Margin="8,0,0,0" MinWidth="84" Padding="12,5" IsCancel="True" Content="Cancel" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
