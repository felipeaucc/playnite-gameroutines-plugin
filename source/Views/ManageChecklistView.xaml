<UserControl x:Class="GameRoutines.ManageChecklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GameRoutines"
             Foreground="{DynamicResource TextBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../GameRoutinesIconResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="ChecklistIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="Padding" Value="2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource ControlHoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.65" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            </Style>
            <Style x:Key="CompactHeaderCheckBoxStyle"
                   TargetType="CheckBox"
                   BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <DockPanel>
                                <Border x:Name="BulletBorder"
                                        DockPanel.Dock="Left"
                                        VerticalAlignment="Center"
                                        Background="{DynamicResource ControlBackgroundBrush}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="{DynamicResource ControlCornerRadiusSmall}">
                                    <Grid Width="16" Height="16">
                                        <Viewbox x:Name="CheckIcon" Visibility="Collapsed">
                                            <Path Data="{StaticResource GameRoutinesCheckGeometry}"
                                                  Fill="{DynamicResource SelectedBrush}"
                                                  Stretch="Uniform" />
                                        </Viewbox>
                                        <Viewbox x:Name="IndeterminateIcon" Margin="2" Visibility="Collapsed">
                                            <TextBlock HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       FontFamily="Marlett"
                                                       Foreground="{DynamicResource GlyphBrush}"
                                                       Text="g" />
                                        </Viewbox>
                                    </Grid>
                                </Border>
                                <ContentPresenter Margin="7,0,0,0"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True" />
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsChecked" Value="{x:Null}" />
                                    <Condition Property="IsThreeState" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="IndeterminateIcon" Property="Visibility" Value="Visible" />
                            </MultiTrigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible" />
                                <Setter Property="Foreground" Value="{DynamicResource SelectedBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BulletBorder" Property="BorderBrush" Value="{DynamicResource ControlHoverBorderBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="BulletBorder" Property="BorderBrush" Value="{DynamicResource ControlSelectedBorderBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasRoutines}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="20"
                       FontWeight="SemiBold"
                       Text="{Binding GameName, Mode=OneWay}"
                       TextTrimming="None"
                       TextWrapping="Wrap" />
            <Grid Margin="0,7,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Text="Routine:" />
                    <TextBlock Grid.Column="1"
                               Margin="7,0,0,0"
                               VerticalAlignment="Center"
                               FontSize="14"
                               Text="{Binding SelectedRoutine.Name, Mode=OneWay}"
                               TextTrimming="None"
                               TextWrapping="Wrap" />
                </Grid>
                <CheckBox Grid.Column="1"
                          Margin="24,0,0,0"
                          VerticalAlignment="Center"
                          Style="{StaticResource CompactHeaderCheckBoxStyle}"
                          IsChecked="{Binding CountTowardOverallTaskStatus, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          Content="Count this routine toward overall task status" />
            </Grid>
            <local:RoutineScheduleEditor Margin="0,2,0,0"
                                         LabelColumnWidth="110"
                                         DataContext="{Binding SelectedRoutine}"
                                         ScheduleChanged="RoutineScheduleEditor_ScheduleChanged">
                <local:RoutineScheduleEditor.Style>
                    <Style TargetType="{x:Type local:RoutineScheduleEditor}">
                        <Style.Triggers>
                            <Trigger Property="DataContext" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </local:RoutineScheduleEditor.Style>
            </local:RoutineScheduleEditor>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasRoutines}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="34" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0"
                       Margin="0,0,6,4"
                       FontSize="11"
                       Opacity="0.7"
                       Text="New item" />
            <TextBox x:Name="NewItemTextBox"
                     Grid.Row="1"
                     Grid.Column="0"
                     Margin="0,0,6,0"
                     MaxLength="120"
                     Padding="7,5"
                     VerticalContentAlignment="Center"
                     ToolTip="Checklist items can contain up to 120 characters."
                     Text="{Binding NewItemText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PreviewKeyDown="NewItemTextBox_PreviewKeyDown" />
            <Button Grid.Row="1"
                    Grid.Column="1"
                    Style="{StaticResource ChecklistIconButtonStyle}"
                    ToolTip="Add Item"
                    Command="{Binding AddItemCommand}">
                <Path Width="12"
                      Height="12"
                      Data="{StaticResource GameRoutinesPlusGeometry}"
                      Style="{StaticResource GameRoutinesButtonIconStyle}" />
            </Button>
        </Grid>

        <Grid Grid.Row="4" MinHeight="120">
            <ListBox ItemsSource="{Binding Items}"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ListBox.Style>
                    <Style TargetType="ListBox" BasedOn="{StaticResource {x:Type ListBox}}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasRoutines}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Style>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="34" />
                                <ColumnDefinition Width="34" />
                                <ColumnDefinition Width="34" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Margin="0,0,6,0"
                                     MaxLength="120"
                                     Padding="6,4"
                                     VerticalContentAlignment="Center"
                                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                     PreviewKeyDown="ItemTextBox_PreviewKeyDown"
                                     PreviewLostKeyboardFocus="ItemTextBox_PreviewLostKeyboardFocus" />
                             <Button Grid.Column="1"
                                     Style="{StaticResource ChecklistIconButtonStyle}"
                                     ToolTip="Move Item Up"
                                     Command="{Binding DataContext.MoveItemUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     CommandParameter="{Binding}">
                                 <Path Width="12"
                                       Height="12"
                                       Data="{StaticResource GameRoutinesChevronUpGeometry}"
                                       Style="{StaticResource GameRoutinesButtonIconStyle}" />
                             </Button>
                             <Button Grid.Column="2"
                                     Style="{StaticResource ChecklistIconButtonStyle}"
                                     ToolTip="Move Item Down"
                                     Command="{Binding DataContext.MoveItemDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     CommandParameter="{Binding}">
                                 <Path Width="12"
                                       Height="12"
                                       Data="{StaticResource GameRoutinesChevronDownGeometry}"
                                       Style="{StaticResource GameRoutinesButtonIconStyle}" />
                             </Button>
                             <Button Grid.Column="3"
                                     Style="{StaticResource ChecklistIconButtonStyle}"
                                     ToolTip="Delete Item"
                                     Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     CommandParameter="{Binding}">
                                 <Path Width="12"
                                       Height="12"
                                       Data="{StaticResource GameRoutinesMinusGeometry}"
                                       Style="{StaticResource GameRoutinesButtonIconStyle}" />
                             </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="0.7"
                       TextAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="No checklist items yet" />
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasRoutines}" Value="False">
                                <Setter Property="Text" Value="No routines configured." />
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasRoutines}" Value="True" />
                                    <Condition Binding="{Binding Items.Count}" Value="0" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

    </Grid>
</UserControl>
