<UserControl x:Class="WeeklyManager.ManageChecklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Text="{Binding GameName}"
                   TextTrimming="CharacterEllipsis" />
        <TextBlock Grid.Row="1"
                   Margin="0,3,0,0"
                   FontSize="15"
                   Opacity="0.8"
                   Text="Manage Checklist" />

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewItemTextBox"
                     MaxLength="120"
                     Padding="7,5"
                     VerticalContentAlignment="Center"
                     ToolTip="Checklist items can contain up to 120 characters."
                     Text="{Binding NewItemText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PreviewKeyDown="NewItemTextBox_PreviewKeyDown" />
            <Button Grid.Column="1"
                    Margin="8,0,0,0"
                    MinWidth="84"
                    Padding="12,5"
                    Content="Add item"
                    Command="{Binding AddItemCommand}" />
        </Grid>

        <Grid Grid.Row="4" MinHeight="120">
            <ListBox ItemsSource="{Binding Items}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem"
                           BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="4" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     MaxLength="120"
                                     Padding="6,4"
                                     VerticalContentAlignment="Center"
                                     Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=Explicit}"
                                     PreviewKeyDown="ItemTextBox_PreviewKeyDown"
                                     PreviewLostKeyboardFocus="ItemTextBox_PreviewLostKeyboardFocus" />
                            <Button Grid.Column="1"
                                    Margin="6,0,0,0"
                                    MinWidth="42"
                                    Padding="6,3"
                                    Content="Up"
                                    Command="{Binding DataContext.MoveItemUpCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Grid.Column="2"
                                    Margin="4,0,0,0"
                                    MinWidth="48"
                                    Padding="6,3"
                                    Content="Down"
                                    Command="{Binding DataContext.MoveItemDownCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Grid.Column="3"
                                    Margin="4,0,0,0"
                                    MinWidth="54"
                                    Padding="6,3"
                                    Content="Delete"
                                    Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Padding="12" IsHitTestVisible="False">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Items.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.7"
                           Text="No checklist items yet."
                           TextAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
