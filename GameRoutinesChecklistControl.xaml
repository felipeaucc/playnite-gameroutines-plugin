<playnite:PluginUserControl x:Class="GameRoutines.GameRoutinesChecklistControl"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:local="clr-namespace:GameRoutines"
                            xmlns:playnite="clr-namespace:Playnite.SDK.Controls;assembly=Playnite.SDK">
    <playnite:PluginUserControl.Style>
        <Style TargetType="{x:Type local:GameRoutinesChecklistControl}">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsTracked}" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </playnite:PluginUserControl.Style>

    <local:GameChecklistView ShowOpenWindowButton="True" />
</playnite:PluginUserControl>
